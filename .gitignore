# Go
/holodex
/holodex.exe
*.test
*.out

# Data / runtime
/data/
*.db
*.db-wal
*.db-shm

# Frontend
/web/node_modules/
/web/dist/
/web/.svelte-kit/

# Test fixtures (generated; keep generator + goldens)
/testdata/mp4/
/testdata/mkv/

# Local config / overrides
/holodex.yaml
/docker-compose.override.yml

# Secrets / personal — never commit
.env
.env.*
!.env.example
.claude/settings.local.json
# Local dev-server config (machine-specific media paths)
.claude/launch.json

# Personal productivity workspace — not committed.
# (Project governance lives in .claude/CLAUDE.md; personal memory lives in ~/.claude.)
/TASKS.md
/dashboard.html
/CLAUDE.md
/memory/

# Editor / OS
.DS_Store
.idea/
.vscode/
