# Entity components

Entity-generic (person | studio | tag) building blocks: searching for an entity, merging one
into another, and the shared video-list body for an entity's detail page.

| File | Purpose |
|---|---|
| `CategoryPicker.svelte` | Category assign/remove picker (HOLODEX-240): search-or-create (`mode="add"`) or search-only (`mode="remove"`) a category for the selected tags — single-step, no informed confirm (assign/remove are reversible, unlike merge). |
| `CollisionOfferCard.svelte` | Video composite-key collision verdict card (HOLODEX-270): "View existing video" / "Save anyway, keep both" — no merge verb, since two video files can't be folded into one record. Video-flavored sibling of `MergeOfferCard`, shown in `NameEditControl`'s same verdict slot on the Video Title mount. |
| `CompletenessSortToggle.svelte` | Owner-only "Completeness ↓/↑" segmented toggle (F55.5) for the People/Studios indexes — kept separate from `SortToggle` since its `PeopleTagSort` type is shared with the Tags page, which has no completeness scoring. |
| `EntityPicker.svelte` | Merge picker: search/pick another entity, then an informed confirm showing both video counts (never a silent merge). Generalized from F23's PersonPicker. |
| `EntityPickerDialog.svelte` | Local entity-search picker for the Extraction tab's People/Studio field edits — searches the app's own entities (no external round trip); confirming never writes, just hands back a name. |
| `EntityVideoMeta.svelte` | The muted video-count line under an entity's title, with its sorted provider-link badges (HOLODEX-266, ADR-083 D3/DD1) appended after a `·` separator. Shared by `EntityVideos`' own default title block and the person page's `hero` snippet, which renders its own title/portrait layout but wants this identical meta row beneath it. (A `ref` prop that appended a `RefChip` was added by HOLODEX-374 and removed the same epic — owner ruling: a `kind:id` pill on the page is not useful; the handle lives in the API/MCP payloads only. Don't re-add it here.) |
| `EntityImageSlot.svelte` | Upload/replace/remove control for one of an entity's single-slot image roles (HOLODEX-286) — generalized from the byte-identical Studio (F51, icon/logo/poster) and Film (F56/HOLODEX-280, poster/thumb) versions. Generic over the role string (`generics="TRole extends string"`); the caller passes its own `api.uploadXImage`/`api.deleteXImage` as the `upload`/`remove` props. `fit` (F59/ADR-089 D4) selects `object-contain` (default) or `object-cover`: contain suits a slot whose ratio matches its art (a 2:3 poster in a 2:3 frame), cover is for one that deliberately differs — the film banner shows a ~16:9 backdrop in an 8:3 band, which contain pillarboxes against the light plate. **No plate under an image (HOLODEX-437, `docs/design/image-plate-handoff.md`)**: the wrapper is `bg-logo-plate` only when the slot is empty (monogram); a contained image sits bare with the `.logo-halo` and a transparent `p-1` (room for the halo inside the `overflow-hidden` box — it used to be the visible cream frame, `p-1` row / `p-0.5` hero); cover has no inset and no halo. **Halo is the caller's choice (HOLODEX-463, ADR-109):** pass `halo` (the saved `HaloMode[]`) and the image wears `.halo-<mode>` only — the studio page does, default off — and add `onhalo` to give the owner the `Halo · <mode>` switch (saves for `theme.mode`); omit both and a contained image keeps the unconditional `.logo-halo` (film slots). No gallery/promote/viewer, unlike Person's image system (`person/PersonGallery.svelte` et al.), which stays separate since it has a capped gallery, dedup, and promote this simple "one row per role" shape doesn't. |
| `EntityVideos.svelte` | Shared body for person/[id], studio/[id], and tag/[id] detail pages: back-link, title, video count, the video grid, scroll restoration (HOLODEX-248, keyed by the `scrollKey` prop), and NS6 in-place video-title filtering off the shared nav search box (HOLODEX-249) — all three callers pass their raw `videos`/`empty`, this component owns the `navSearch`-driven filtering and empty-state swap. Optional `hero`/`detail`/`footer` snippets — `footer` (F56) renders after the grid, used for the films row. Wraps `VideoGrid` in `id="videos"` (`scroll-mt-16`) — the F68 hover card's Videos link lands there. |
| `FilmsRow.svelte` | Films row (F56, design handoff §5): small horizontal shelf of poster-thumb cards linking to each film, passed into `EntityVideos`' `footer` snippet by person/studio/tag detail pages. Each card draws `poster_url` when present and the monogram only as the empty state — never the monogram unconditionally (HOLODEX-383, the same defect as HOLODEX-318 on the films index and HOLODEX-329 on media-detail chips; the list read already fills `poster_url`). Renders nothing when `films` is empty — callers gate on `films_enabled` + non-empty before passing data, never an informational dead end. **A film tile is a video card's sibling, not a chip (HOLODEX-384):** its poster is exactly as tall as the grid's `.video-frame` above it — the shelf is a `.films-shelf` size container (app.css) fed `--cols` from the same `effectiveDensity()` VideoGrid renders with and `data-layout` from `activity.cardLayout`, so wide layout gives a 3/8-card-wide tile and poster layout a full column — and it wears the frame's `border-rule`/`rounded-theme`, VideoCard's `p-3`/`text-sm`/`skin-title` caption block, and the shared `.media-lift` hover hook (the person hero's, renamed). The `<ul>` carries `p-2 -mx-2 -mb-2` because a scroll container clips the 6% lift on every edge. Reads density itself rather than taking a prop: `EntityVideos`' grid is never stage-aligned, so the plain track width is the right target. The section carries `id="films"` (`scroll-mt-16`) — the F68 hover card's Films link lands there. |
| `MergeCanonicalDialog.svelte` | Step two of a multi-select merge: "keep which name?" — the rest fold into the survivor. Shared by /people and /tags. |
| `DisplayNameLine.svelte` | The name field's curation line under a Person/Studio/Film heading (F60 RD10, HOLODEX-378): "In files as `<canonical>`" (films: "On record as") plus the name field's `SourceBadge` for the owner, rendered only when the resolved spelling differs from the canonical column — **owner-only**: visitors see the heading alone (owner ruling: they don't need to know about the files; the visitor-facing value is the resolved name, so the routes content rule is satisfied by the heading). With no decision standing the owner gets a quiet link in the same slot that opens the badge's chip row in place — it **names the provider spelling on offer** (`Display as Ana Keßler (tmdb)…`; bare `Display as…` when none differs), because QA 4.3's first pass showed that at rest nothing else says a second spelling exists; focus hands across the link↔badge swap. Two verbs, two affordances: this line is "Display as" (a DB-only decision), the docked pencil beside the heading is "Rename in files" — and once a decision stands the record spelling on this line is the owner's visible trigger for that same form (`onRename` → `NameEditControl.open()`; the pencil is hover-hidden beside a heading that no longer shows the record spelling, QA 4.3 follow-up). Never mount the name field in a page's field list — this is its one mount. |
| `MergeOfferCard.svelte` | Homonym-collision verdict card (HOLODEX-269, extracted from `AliasPanel`): "is this the same {noun}?" merge-in / keep-separate choice, shown on an exact-name collision from an alias add or a rename. Presentational only — caller owns busy/error state and the merge/keep-separate calls. Shared by `AliasPanel`'s alias-add flow and `NameEditControl`'s rename flow. |
| `NameEditControl.svelte` | Docked-pencil rename affordance (HOLODEX-269): at rest, renders identically to a non-owner's view; on owner hover/focus, a low-opacity pencil brightens, opening an inline rename. `pencilAlwaysVisible` (Video Title only) skips the hover/focus reveal and keeps the pencil fully opaque at rest — Person/Studio/Tag headers omit it and keep the default hover reveal. On a 409 collision, shows a caller-supplied `verdict` snippet (typically `MergeOfferCard`, or `CollisionOfferCard` for Video) inline instead of a modal. Generic over the conflict payload (HOLODEX-270, `TConflict = EntityRef` by default) so Video Title can supply `VideoCollisionRef` with no change to the Person/Studio/Tag call sites. Shared by Person/Studio/Tag detail-page headers, the Video Title heading, and the Film year (F59/HOLODEX-317). **Not name-only despite the name:** the Film year mount is the first caller that is neither a name nor the page heading, and it drove three optional, defaulted widenings — `as` (the resting element, default `h1`; the year passes `p`, since the film title above already owns the page's single h1 and a year is a field value, not a heading), `editLabel` (overrides the `Rename this {label}` accessible name, which describes no action for a year), and `placeholder` (resting text when the value is empty, e.g. "No year set" — the edit input still opens empty, so the placeholder never becomes a value the owner must delete first). Every original call site is unchanged. `editValue` (HOLODEX-378) is the value the edit form opens with when the heading is not the canonical value: Person/Studio/Film headings render the *resolved* name (a display decision may pick a provider or custom spelling) while the pencil renames the canonical record, so they pass `name={displayName} editValue={canonical}` and "unchanged" is judged against `editValue`. `open()` (exported, HOLODEX-378) opens the same form from outside via `bind:this` — `DisplayNameLine` is the one caller. Before this, the Film page **hand-rolled** `name-edit-row`/`name-edit-pencil` for its studio row without importing the component — if you are about to copy those classes, mount this instead. |
| `PeopleGrid.svelte` | Reusable People/Cast grid section: title + a flex-wrap row of `PosterTile`s (the same tile Films draws) + optional owner editing. Extracted from the Media detail page's People section; the Film detail page's Cast section (a read-only derived union of its scenes' people) now renders through the same component by omitting `attach`/`detach` — editing (per-tile remove badge + `PersonPicker` add tile) only appears when both are supplied. Owns the whole section including its `{#if}` visibility gate, not just the read-only tile shape (unlike `StudioLinkCard`, which stays a bare per-item card beside a separately-mounted `StudioPicker`) — People is a multi-item grid with per-item remove baked in, closer to Film's own tile shape than to Studio's single-value field. |
| `PersonPicker.svelte` | People relationship-edit popover (HOLODEX-272), mounted by `PeopleGrid` rather than directly by pages: a `PickerShell` popover opened from either a docked "Add person" dashed poster-tile (`hasPeople=true`, sits inside the grid beside real poster tiles) or a bare `+ Add person` text CTA (`hasPeople=false`, HOLODEX-289-style — an empty grid's lone dashed tile with nothing beside it read as less fluid than text) — the multi-select sibling of `StudioPicker` (shows the video's already-attached people, each independently removable, alongside search/create). Every result row requires an explicit Actor/Director role choice before commit; stays open across commits instead of closing on the first one. Commits through the video detail page's `attachPerson`/`detachPerson` functions (`media/[id]/+page.svelte`), passed through `PeopleGrid`, which `POST /media/{id}/curation`. Unlike `StudioPicker` it has no `verdict` prop — a conflict just closes the popover, and the page renders the shared `CollisionOfferCard` where the tile was, since the grid's own remove control shares the same attach/detach calls with no `PersonPicker` instance to render into. |
| `PickerShell.svelte` | Shared dialog chrome for `EntityPicker`/`CategoryPicker`: backdrop, `role="dialog"` wrapper, focus trap, trigger-focus save/restore, Escape-to-close, rise-in animation. Title/body are step-specific, passed as snippets. |
| `StudioLogoBox.svelte` + `studioLogo.ts` | The one studio image box (HOLODEX-432, extracted from `StudioLinkCard`), shared by the card and `StudioListRow`: `logo_url` → `icon_url` → monogram (HOLODEX-397 — logo first because it is the only role TMDB enrichment fills). Fixed-height, width from the image's own aspect clamped to `h-12 min-w-12 max-w-48` with `object-contain` — never a cover-crop (see "Frame follows source aspect" below); dashed border only when no image at all. **No plate under any image** (HOLODEX-411 for logos, HOLODEX-437 for icons — the cream box read as a badge on the dark skins and as bulk everywhere): logo and icon both sit bare and may wear the halo (app.css hook, HOLODEX-432 owner ask) — since HOLODEX-463 (ADR-109) only when the owner turned it on for the shown role in the current palette mode (`studio.image_halo`, default off) — a `drop-shadow` in `--logo-halo` (the plate on dark, black on light) that traces the mark's silhouette so a dark mark reads on a dark row — the plate's job without the plate. The `p-1` is transparent room for the halo inside the `overflow-hidden` box, not a visible border. Only the **monogram** keeps the (dashed) plate — it is text and needs a ground. The **caption rule** lives in `studioLogo.ts` (`isWordmark` = natural w ≥ 2h; `showName`; `imageAlt`): the box binds `wordmark` **out** (`null` until the logo has loaded, `false` on error) and the caller decides whether to render the name beside it; the box's own `alt` is the name exactly when the caller does *not* — the name is announced once, never twice. Don't inline this box or the rule elsewhere; mount the component and bind. |
| `StudioLinkCard.svelte` | Reusable read-only studio display (HOLODEX-290) for the Media and Film detail pages: `StudioLogoBox` + linked name + video count, one card per studio. **A wordmark logo renders without the name + count caption** (HOLODEX-411, `docs/design/studio-logo-link-card-handoff.md`) — they move to the image `alt` and the link `title`; a squarer logo, the icon and the monogram keep the caption, and nothing renders beside a logo until its natural size is known. Replaces the ad hoc comma-joined text links the Media and Film detail pages each grew independently — those pages keep their own `StudioPicker`/`FilmStudioCascadeDialog` edit affordances beside it unchanged. No owner/edit/busy state of its own; takes a `Studio` as-is. |
| `StudioListRow.svelte` | One `/studios` index row (HOLODEX-432, `docs/design/studio-list-logo-handoff.md`): the bordered row — a positioned wrapper, a stretched `<a>` (F65.8: the completeness ring is a `<button>`, so it sits beside the link, `z-[1]` above its `::after` stretch, never inside it) with `StudioLogoBox` in front of the name, then the owner-only `CompletenessRing` and the video count — the same caption rule as the card (owner's pick B over a list-only fixed slot), so a wordmark studio's row shows the logo alone with its name in `alt` + `title`. The empty name `<span>` stays as the `flex-1` spacer. Exists as a component only because the wordmark decision is per-row state an `{#each}` body can't hold; `eager={i < 6}` mirrors the People list's avatar loading. |
| `StudioPicker.svelte` | Studio relationship-edit popover (HOLODEX-271): docked-pencil affordance replacing `SourceSelect`'s Studio radiogroup on the video detail page — known-candidate chips (via `sourceChips`), full-library search, and an inline create-fallback, all committing through the video detail page's `decideStudio`/`saveStudioAnyway` functions (`media/[id]/+page.svelte`), which call `PUT /media/{id}/fields/studio/decision`. Runs the same HOLODEX-270 collision check as Title (via its caller-supplied `decide`) and shows `CollisionOfferCard` through the same generic verdict-snippet mechanism `NameEditControl` uses. The trigger itself branches on the caller-supplied `hasStudio` (HOLODEX-289): the docked pencil once a studio is linked, or a `+ Add studio` text CTA (matching Tags' `+ Add tag`) when none is — a bare pencil floating with no linked name for visual anchor was undiscoverable. |
| `TagLinkChip.svelte` | Reusable tag display (HOLODEX-292): linked name + provenance suffix (`·file`/`·manual`/`·provider`) + optional remove control, one chip per tag. Owner vs. read-only is decided by whether the caller passes `onremove` (no separate `isOwner` boolean). Replaces three previously-inconsistent inline chip styles the Media (owner and visitor branches disagreed with each other) and Film detail pages each grew independently; both now share the same `rounded-full border-rule bg-surface-2` shape. The add-tag control itself (search/create popover) stays a separate component, `TagPicker.svelte` (HOLODEX-287/ADR-088) — this component owns only the per-tag chip, not the "+ Add tag" affordance. |
| `PosterTile.svelte` | The shared 2:3 poster chip (HOLODEX-296): fixed `w-20` `curation-chip` `<li>`, linked poster + two-line caption, and the hover-reveal remove badge as a *sibling* of the `<a>` (never nested). The Media detail page's Films row and `PeopleGrid`'s People row both render through it — those two rows sit side by side and must stay the same tile (HOLODEX-328), so a size or hover change lands here once, not in two copies. Owner vs. read-only follows the `TagLinkChip` rule: pass `onRemove` or don't — no `isOwner` boolean. `poster` draws only the image box (caption comes from `name`); `children` is the extra-overlay slot Films uses for its scene pill, and `removeSide="left"` exists only because that pill owns the top-right corner. **F68:** an opt-in `personId` wraps the tile's link in `person/PersonLinkChip` (`wrapClass="relative block"`) so the tile carries the person hover card; `PeopleGrid` sets it, film tiles never do; the remove badge stays a sibling outside the chip. |
| `SearchResultsPanel.svelte` | Grouped/tabbed search results (NS1/NS5, HOLODEX-249) — the All/People/Videos/Studios/Tags tab row + capped result rows, roving tabindex. Shared by the nav box's live-typing dropdown (`variant="dropdown"`) and `/search`'s page body (`variant="page"`), per "reuse, don't fork." Spans video results too, not just person/studio/tag. `showResults=false` (NS2) renders only the tab row — used while a scoped page (Media/People/Studios/Tags) is filtering its own grid in place, so the owner can still tap another tab to preview it without the results body doubling what the page is already showing. **F68:** people rows carry `personId`, and in the `page` variant only the row is wrapped in `person/PersonLinkChip` (the row's own `<a>` — option role, roving tabindex, handlers — untouched inside); the `dropdown` variant never mounts a card (RD6, popover-in-popover). |
## Rules

- **`NameEditControl`'s `trailing` slot is for content that never needs to wrap** (a flag row, a
  short badge). Its at-rest heading row is deliberately `flex` *without* `flex-wrap` — HOLODEX-356:
  the name breaks words and the pencil stays docked — so anything in that slot can only take width
  *from* the heading; a wrapping pill there squeezed a four-letter title to one letter per line at
  375px (HOLODEX-377). Content that should drop beneath the title when it doesn't fit goes in a
  flex-wrap row the *page* owns around the control (media page's edition pill is the model: the
  control in a `min-w-0 max-w-full` item, the pill `shrink-0 max-w-full wrap-anywhere`).

### Frame follows source aspect, never config or role name

Never cover-crop an image into a frame its source can't fill. An image's aspect ratio is a
property of the *bytes*, not of the role it was stored under or a config flag the viewer set.
This has been decided twice, both times after shipping the defect (HOLODEX-385/386, 2026-09-15):

- `card_layout: poster` forced *video* cards (scene thumbnails — frame grabs, inherently
  wide) into a 2:3 frame. Only ever looked right when the thumbnail was secretly the film's
  cover art. Removed by HOLODEX-385; the films index owns poster-shaped cards.
- A provider sidecar emitted the film *poster* under the `banner` kind; `EntityImageSlot`
  `fit="cover"` in an 8:3 band then showed a cropped slice of portrait art. HOLODEX-386 refuses
  a portrait image for the landscape role at ingest rather than removing the band.

So: `fit="cover"` is for a slot whose ratio *deliberately* differs from art of a *known*
aspect (a ~16:9 backdrop in an 8:3 band). If the source aspect is not guaranteed by a check
upstream — ingest refusal, upload validation, or a `naturalWidth`/`naturalHeight` gate —
use `contain`, or don't render the frame at all. Don't add a layout flag to paper over a
mismatch; fix the source or the gate. When a frame has nothing that fits it, render nothing
(F25.30's "no band when empty") rather than a plate or a crop.
