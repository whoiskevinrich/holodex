# Capture inbox — append-only; `/triage` drains it into Jira

- [2026-09-20, from HOLODEX-431 / F68] Geometry harness: add a *hover preparation* (dispatch `pointerenter` on a `[data-person-chip]`, wait ≥ 300 ms; keyboard focus needs `document.hasFocus()`) so the person hover card's layout invariants become §12 rungs at the 8-column cell on a film page with a Cast grid — `scrollWidth === clientWidth` with the card open at a right-column tile (HOLODEX-356 guard) and `card.right ≤ innerWidth − 16` (the RD10 clamp); mutation-test by zeroing `shiftX`. Pure `placeCard` maths already under Vitest (`web/src/lib/components/person/personCard.test.ts`); this is the rendered half. Task · Low · labels frontend, testing, geometry-harness. Captured here because both Atlassian connectors were down.
- [2026-09-20, from HOLODEX-431 / F68] Person hover card: alias overflow "+N more" — the card shows the first three aliases with no indication more exist (spec P1); render "+N more" as a link to the profile's Aliases section (`/people/{id}#aliases`, anchor to add), same `text-accent` treatment as the row links. Story · Low · labels frontend, ux. Captured here because both Atlassian connectors were down.
