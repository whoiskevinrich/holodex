# Architecture Decision Records

Index of ADRs for Holodex. Each records one decision, its rationale, and consequences.

| ADR | Decision | Status |
|-----|----------|--------|
| [001](ADR-001-backend-language.md) | Backend language — **Go** | Accepted |
| [002](ADR-002-frontend-framework.md) | Frontend framework — **SvelteKit** (SPA) | Accepted |
| [003](ADR-003-database.md) | Database — **SQLite** (modernc) + FTS5 + WAL | Accepted |
| [004](ADR-004-metadata-extraction.md) | Metadata extraction — **exiftool + ffprobe + ffmpeg** | Accepted |
| [005](ADR-005-mcp-transport.md) | MCP transport — **HTTP/SSE primary**, stdio secondary | Accepted |
| [006](ADR-006-api-design.md) | API design — **REST + OpenAPI 3.1** (chi) | Accepted |
| [007](ADR-007-docker-structure.md) | Docker — **single multi-stage image** + Vite dev | Accepted (embed/build mechanics → ADR-020) |
| [008](ADR-008-caching.md) | Caching — **ristretto** in-process, Redis-ready | Accepted (in-process backend deferred → ADR-022) |
| [009](ADR-009-thumbnail-strategy.md) | Thumbnails — tiered, throttled, priority-aware backfill | Accepted |
| [010](ADR-010-mkv-tag-precedence.md) | MKV tag-target precedence — **level 50** authoritative | Accepted |
| [011](ADR-011-symlink-handling.md) | Symlinks — follow, dedup by canonical path | Accepted |
| [012](ADR-012-resolution-classification.md) | Resolution — **width-based** buckets, 10% tolerance | Accepted |
| [013](ADR-013-metadata-field-mapping.md) | Configurable metadata field mapping | Accepted |
| [014](ADR-014-configuration-and-data-layout.md) | Configuration strategy & data directory layout | Accepted |
| [015](ADR-015-media-file-serving.md) | Media file serving — **HTTP Range** requests | Accepted |
| [016](ADR-016-database-migrations.md) | DB migrations — **golang-migrate**, embedded | Accepted |
| [017](ADR-017-search-architecture.md) | Search — global mixed-entity + FTS5 | Accepted |
| [018](ADR-018-scanner-change-detection.md) | Scanner — incremental by (path, size, mtime) | Accepted |
| [019](ADR-019-observability-conventions.md) | Observability & operational conventions | Accepted |
| [020](ADR-020-frontend-embed-and-build.md) | Frontend embed location, SPA fallback serving & BuildKit caching | Accepted |
| [021](ADR-021-frontend-theming-and-skins.md) | Frontend theming — semantic design tokens & 3-skin system | Accepted |
| [022](ADR-022-defer-in-process-cache.md) | Defer the in-process cache to a measured need | Accepted |
| [023](ADR-023-image-distribution.md) | Image distribution — **published GHCR image + pull-based compose** | Accepted |
| [024](ADR-024-ci-cd-pipeline.md) | CI/CD — **PR gate + supply-chain scanning + tag-driven releases** | Accepted |
| [025](ADR-025-tailwind-v4-css-first.md) | Tailwind v4 — **CSS-first config** (`@theme inline`) + Vite plugin | Accepted (supersedes ADR-021 §2 mechanism) |
| [026](ADR-026-metrics-exposition.md) | Prometheus metrics — **hand-rolled exposition, no client_golang** | Accepted (extends ADR-019) |
| [027](ADR-027-dotenv-local-config.md) | Local **`.env` loading** for dev config | Accepted (extends ADR-014) |
| [028](ADR-028-activity-surface-and-job-history.md) | User-facing **activity surface** & **job-history** persistence | Accepted (extends ADR-019; spec F21) |
| [030](ADR-030-access-control-gating-seam.md) | Access-control / **"Pro mode" gating seam** for owner-only surfaces | Accepted (spec F21; `ADMIN_TOKEN` + `requireOwner`) |
| [031](ADR-031-related-media-endpoint.md) | Related-media endpoint — **one combined call + `ORDER BY RANDOM()` seam** | Proposed (spec Quick Wins QW2; extends ADR-006/017) |
| [032](ADR-032-browse-state-preservation.md) | Browse-state preservation — **module-scoped grid+scroll cache for fluid Back** | Proposed (spec Quick Wins QW4; extends ADR-002) |
| [033](ADR-033-metadata-source-plugins.md) | Metadata source plugins — **sidecar providers** over a unified resolution layer | Accepted (spec F22; generalizes ADR-013) |
| [034](ADR-034-release-notes-and-deployments.md) | Release notes — **git-cliff changelog** + GHCR **deployment linkage** + package link | Accepted (extends ADR-023/024) |
| [035](ADR-035-ci-cd-scoping-and-release-gate.md) | CI/CD refinements — **image path-scoping** + CodeQL concurrency + **release test-gate** | Accepted (extends ADR-023/024) |
| [036](ADR-036-person-alias-search-indexing.md) | Person aliases — **dedicated `person_aliases_fts` mirror** (not a denormalized column) | Accepted (spec F23; extends ADR-017) |
| [037](ADR-037-soft-delete-and-purge.md) | Soft-delete media — **`deleted_at` axis orthogonal to `active`** + dedicated purge job | Accepted (spec F24; extends ADR-018/028/030) |
| [038](ADR-038-person-images.md) | Person images — **on-disk store + typed real-or-placeholder serving** + shared ingest normalization | Proposed (spec F25; extends ADR-009/030/033) |
| [039](ADR-039-provider-asset-urls.md) | Provider asset URLs — contract clarification + **operator-configured `asset_hosts` allowlist** | Accepted (extends ADR-033/038; updates metadata-provider-contract) |
| [040](ADR-040-tmdb-provider-repo-placement.md) | TMDB provider source placement — **monorepo `providers/tmdb/` subdirectory** | Accepted (extends ADR-033/039/023/024) |
| [041](ADR-041-metadata-writeback.md) | Metadata writeback — **explicit per-field write-back** to media files via copy→write→rename | Proposed (extends ADR-004/013/033; spec F28) |
| [042](ADR-042-windows-asInvoker-manifest.md) | Windows build — **`asInvoker` application manifest** embedded via `.syso` to suppress UAC prompt | Accepted (spec windows-uac-manifest) |
| [043](ADR-043-gallery-cap-and-enrichment-suppression.md) | Person gallery — **configurable cap (`PERSON_GALLERY_MAX`) + owner over-cap override + enrichment URL suppression on delete** | Accepted (spec F25.23–25; extends ADR-038/039) |
| [044](ADR-044-automated-version-and-release-pr.md) | Release versioning — **Release Please** release-PR + PAT-driven tag in front of unchanged `release.yml` | Accepted (extends ADR-024/034/035; revisits ADR-034's "no release-please") |
| [045](ADR-045-seeded-random-ordering.md) | Seeded random ordering — **deterministic `holo_shuffle(id, seed)` SQL function** for paginated Media + named-sort enum for People/Tags | Proposed (spec sort-persistence SP2/SP3; extends ADR-006/003/017; relates ADR-031) |
| [046](ADR-046-owner-session-persistence.md) | Owner session persistence — **HttpOnly signed token-exchange cookie** (`POST /session`), gate accepts cookie-or-header | Proposed (spec owner-session-persistence; amends ADR-030's "in-memory only" condition) |
| [047](ADR-047-per-item-metadata-refresh.md) | Per-item metadata refresh — **unified forced re-extract + re-enrich** over a `plan`/`apply` seam; flat `kind=refresh` job row; non-destructive layering invariant | Proposed (spec F31; extends ADR-004/018/033/028/030; relates ADR-013/037/041) |
| [048](ADR-048-metadata-curation-and-write-queue.md) | Granular metadata curation — **cross-source dedup merge + `manual` source/tombstones** + **durable bounded-concurrency batch-writeback queue** | Proposed (spec F30; generalizes ADR-013; extends ADR-033/041/028/030; partially realizes ADR-041 Option C) |
| [049](ADR-049-manual-image-precedence.md) | Owner-set person images — **enrichment never overwrites an `upload`/`promoted` core slot** (provenance-implicit lock, no migration) | Proposed (spec F25.31/F33; extends ADR-038; sibling of ADR-043; image twin of ADR-048) |
| [050](ADR-050-image-content-dedup.md) | Deduplicate enrichment photos by **image content hash** — gallery `extra` skipped when its sha256 matches any of the person's images; `source_url` fast-path; app-layer enforcement + one-time backfill (migration 0015) | Proposed (spec F34; extends ADR-038; byte-level sibling of ADR-043/ADR-049) |
| [051](ADR-051-per-field-source-of-truth-decisions.md) | Per-field **source-of-truth decisions** — file-baseline default + standing per-item `{file, provider:<name>, manual}` decision overriding precedence (source-pin, not value); drives display + writeback; **entity-generic + multi-provider** (migration 0016) | Proposed (supersedes ADR-047 F31.11 slice; extends ADR-013/033/041/048; relates ADR-030/036) |
| [052](ADR-052-baseline-source-contract.md) | **`BaselineSource` contract** — resolver baseline-layer seam: entity-agnostic `ResolveFields` core over a `BaselineSource` interface, `Resolve` reduced to the video (file-layer) wrapper; behavior-preserving | Accepted (realizes ADR-051 §9 fast-follow ①; extends ADR-033/013; spec F36) |
| [053](ADR-053-studio-entity-and-resolved-link-derivation.md) | **Studio entity** — `studios` + `video_studios` join + `studios_fts` (migration 0017); `video_studios` **derived from the resolved `studio` field** (re-linked on scan/enrich/decision/curation, prune-on-empty), not raw extraction; `studioBaseline`; identity ops deferred | Proposed (realizes ADR-051 §9 fast-follow ③; extends ADR-052/017/013; relates ADR-036; spec F38) |
| [054](ADR-054-studio-external-id-dedup.md) | **Studio external-id de-dup** — `studio_external_ids(external_id PK, studio_id FK)` join (migration 0018); capture TMDB `production_companies[].id` via a self-describing **internal sidecar** enrichment field (`_`-prefix, never displayed/resolved); thread a name→external_id side-map into an **external-id-first** `resolveOrCreateStudio` so same-company spellings converge (refines ADR-053 RD1) | Proposed (extends ADR-053; relates ADR-033/036/047; spec F38 P2-5; HOLODEX-122) |
| [055](ADR-055-enrichment-unique-key-invariant.md) | **Universal enrichment unique-key invariant** — every source MUST supply a namespaced id `<namespace>:<id>` and it is the **sole identity/de-dup key** for the entity (no name fallback); the namespace is a **shared identity space** so providers converge cross-provider; generalizes ADR-054 to all canonical entities (studio ✓, person → HOLODEX-125, video enrichment, future tags) | Proposed (generalizes ADR-054; extends ADR-033 contract; relates ADR-036/047/051; HOLODEX-123; impl HOLODEX-124/125) |
| [056](ADR-056-provider-field-render-hints.md) | **Provider field render hints + auto-registration** — additive `/describe.field_hints` map (label/render/group/order) for **non-canonical** keys, no protocol bump; a **four-tier ladder** (operator mapping > code registry > provider hint > title-case) keeps operator/registry authority above the provider; **presence-driven, display-only** auto-registration surfaces stored non-canonical shadow fields on video/person/studio; hints persisted (`provider_field_hints`); `image_url` gated by the ADR-039 asset-host allowlist | Proposed (extends ADR-033/013/052/039; relates ADR-051/055; spec F39; HOLODEX-128) |
| [057](ADR-057-self-hosted-studio-logo.md) | **Self-hosted studio logo** — replace the hotlinked provider-CDN `logo` URL with an on-disk, normalized (`personimage.Normalize`) **cache derived from the resolved `logo` field** (`studio_logos`, migration 0020, one row per studio); `RelinkStudioLogo` re-syncs it on enrich/decision triggers and fetches through the unchanged ADR-039 asset perimeter; served from `/studios/{id}/logo`, monogram as the empty state; no provider/contract change | Proposed (realizes F38 P2-3; reuses ADR-038, passes ADR-039; derived-cache twin of ADR-053; leaves the ADR-051 `logo` field intact; HOLODEX-130) |
| [059](ADR-059-provider-brand-icon.md) | **Provider brand icon** — additive `/describe.brand_icon` asset object advertising a provider's brand mark; core downloads it through the unchanged ADR-039 asset perimeter, `personimage.Normalize`s it, self-hosts one row per provider (`provider_icons`, migration 0021, `?v=id` cache-bust), and serves `GET /providers/{name}/icon` (immutable, 404 → monogram); icon URL exposed on the owner `SourceInfo` **and** a new public `GET /providers` directory for visitor provenance badges; monogram fallback when absent. Per-provider analogue of ADR-057's per-entity studio logo | Proposed (extends ADR-033/039/038; mirrors ADR-057; relates ADR-056; HOLODEX-134; unblocks HOLODEX-135/136/137) |
| [058](ADR-058-jira-transitions-via-rest-api.md) | **Jira transitions via REST API** — retire the metered Jira **Automation** rules (Free plan: 100 flow runs/mo shared site-wide) and fire the four transitions via the **uncounted REST API**; split by event origin: **In Progress** = agent/session (MCP, start-of-work), **In Review/Done/Released** = GitHub Actions (unmetered on public repo); scoped token via the **`api.atlassian.com/ex/jira/{cloudId}` gateway** (site-URL 401s); single token, **scripted two-repo fan-out** rotation. Mirrors Bookshelf BOOKSHELF-84 | Proposed (relates ADR-024/034/035/044; security-review on impl diff) |
| [060](ADR-060-runtime-owner-settings.md) | **Runtime owner-editable settings** — generic DB-backed `settings` KV (migration 0021) + typed **registry** allowlist as a new precedence layer **above** startup config (`DB override ?? config seed ?? built-in default`); in-memory field stays the hot read path, refreshed on write + on boot (`LoadSettingOverrides`); owner-gated `/admin/settings` + new `/owner/settings` tab; **debut wiring = `person_gallery_max` only**. Sidecar per-enrich count deferred | Proposed (extends ADR-014; promotes ADR-043's cap; gate ADR-030; spec F41) |
| [061](ADR-061-unified-entity-name-identity.md) | **Unified entity name-identity** — per-entity normalized `nameKey` (unique across canonical ∪ aliases) + shared alias/merge/keep-separate spine across **Person/Studio/Tag**; fixes the `"fox"`/`"Fox"` case-collision (binary canonical vs NOCASE alias); two-path handling (scan hard-normalizes, editor detect+prompt); tags get identity-only, not the field-decision model; composes with ADR-055 as id→name→create. Backed by prod probe (14 safe pure-case pairs, 56 near-misses ⅔ tags) | Proposed (realizes ADR-053 RD4 + generalizes ADR-036/F23; complements ADR-055; supersedes ADR-053 binary-name identity + tag bare-string; spec F43) |

| [063](ADR-063-derived-computed-fields.md) | **Derived / computed fields** — a *compute-on-read, source-less, read-only* field genre: `registry.FieldDef` gains `Computed`/`DependsOn`; a **pure `Derive(resolved, now)` post-pass** (twin of ADR-056 `AutoRegisterFields`) appends rows from a **closed Go formula registry** (`deriveAge`/`deriveAgeAtDeath`, one function branching on `deathdate`); a **non-adoptable `computed:` provenance token** kept **out of** the `fieldsource` decision grammar (constant/formatter/recognizer only, structural nil-`Decision` + API guard); the **clock is injected at the handler** (`Handlers.now`) so `internal/resolver/` stays clock-free. Seeded with person Age/Age-at-death; entity-generic | Accepted (extends ADR-052/051/013; sibling of ADR-056; refines its own spec FR4; spec F45; HOLODEX-73) |
| [062](ADR-062-in-app-field-promotion.md) | **In-app field promotion** — owner-gated DB override (`field_promotions`, migration 0023) that makes an F39 auto-registered non-canonical field first-class **curatable** without editing `metadata-mappings.yaml`; consulted as a resolver **tier-0** (promotion > operator YAML > registry > provider hint > title-case) — **amends ADR-056 §D2**, inverting "operator YAML wins" for the promoted key (the owner *is* the operator); global-per-`(entity_type, field_key)` presentation, per-entity F36/F30 value curation reused; candidate sources **derived per-entity from shadow provenance** (no stored source list), `Multi = render=="chips"`, `Filterable` stays false (browse-facet deferred); passes ADR-039 `image_url` allowlist; reversible de-promote leaves shadow value + curation intact | Proposed (amends ADR-056; extends ADR-051/048/052/013; gate ADR-030/045; spec F44; HOLODEX-171) |
| [064](ADR-064-flightplan-plugin.md) | **Flightplan** — a portable Claude Code plugin that converts this repo's hand-rolled per-epic worklogs into config-driven state: on-disk worklog (gates + up-next + session log) as ground truth, mechanical `SessionStart`/`PostToolUse(Skill)`/`Stop` hooks (never judgment — Hook Safety rule), `/handoff` skill owns the `[x]` "done" call, offline `INBOX.md` + `/triage` for idea capture; `.claude/flightplan.yaml` is the portability seam | Proposed (extends ADR-058; relates ADR-044; two-batch POC; HOLODEX-182) |
| [065](ADR-065-typed-field-registry-and-relationship-scoped-computed-fields.md) | **Typed field registry + relationship-scoped computed fields** — `registry.FieldDef` gains a `FieldType` (text/categorical/numeric/date) + inert `OperatorsByType` (equals/contains/range) taxonomy, registered on a minimal proving set only; a **new, parallel `DeriveRelationship(person, video, now)` pass** — `Derive`'s two-entity twin, not a bent version of it — computes `age_in_media` from a `PersonDependsOn`/`VideoDependsOn` marker; output has no home in either entity's own `resolved[]`, so `getMedia` attaches it as a new top-level `related_person_fields` payload key (avoids a `model`↔`resolver` import cycle); birthdate reused via the existing `personResolved` per cast member for decision/curation correctness | Superseded — HOLODEX-176 closed Won't Do (zero shipped consumers; Age in Media shipped instead as a narrow bespoke join, HOLODEX-173); revisit only if HOLODEX-180 needs the substrate |
| [066](ADR-066-enrichment-auto-apply-and-dismissal.md) | **Enrichment auto-apply-with-revert** — a single-strong-match (`confidence >= 0.85`) `/resolve` result routes straight to the existing `apply()` path, no confirm click; amends the provider contract's §2.3 "never auto-applies in v1" posture (documentation-only, no wire-format change); auto-applied values inherit F36 Keep/Revert unchanged, no new undo mechanism; a new durable `enrichment_dismissals(entity_type, entity_id, provider)` store records an owner's "not matched" verdict, structural sibling of ADR-061's `entity_keep_separate` | Proposed (extends ADR-033 contract; relates ADR-051/055/061; spec F47; HOLODEX-186) |
| [067](ADR-067-filename-extraction-confidence-and-rollback.md) | **Filename extraction + confidence-gated auto-apply + rollback** — generalizes ADR-066's auto-apply to a second, different-scoring candidate source (filename parsing), with multi-component per-field-type rubric + hard exact-match gate for entities + Jaro-Winkler fuzzy suggestions (advisory only); reopens ADR-041's deferred "prior-value capture" via append-only `file_writeback_snapshots` + inverse-operation revert (revert-as-forward-write, reuses F30 queue) | Proposed (extends ADR-033/041/048/066; amends ADR-041 non-goal; spec F48; HOLODEX-190) |
| [068](ADR-068-extraction-resolve-entity-materialization.md) | **Extraction resolve materializes entities via post-write re-extract** — a resolved "create new" Person/Studio is created by re-reading the just-written file through the canonical `UpsertVideo` path (new file-only `refresh.ReExtract`, wired into the write-queue post-write hook, gated to entity-field writes whose source is `filename`/`manual` — skips `merge`/`revert`), not an inline insert, keeping the file the single source of truth; multi-person review stays one scalar row carrying per-value candidates (no migration) so the UI renders per-person click-to-edit chips and editing one name can't collapse the cast | Proposed (extends ADR-067/041/047/048; relates ADR-033/051/052/061; spec F48; HOLODEX-196) |
| [069](ADR-069-draft-prs-for-pre-implementation-gates.md) | **Draft PRs carry pre-implementation gates** — a pre-implementation gate artifact (ADR, spec, design handoff) is published as a **Draft PR** as soon as it lands, so it gets a reviewable diff, CI, and a Jira dev-panel link without claiming the *work* is in review; **a Draft PR fires no transition** and `In Review` moves from `pull_request: opened` to *ready for review* (`opened` when not draft, or `ready_for_review`), gated in the workflow `if:` so `jira-branch-sync.mjs` stays a pure transition-the-branch's-keys script. One PR matures Draft → ready → merge, so GitHub's own merge block — not a label or trailer — is what keeps `Done` from firing early | Proposed (amends ADR-058's In Review trigger; relates ADR-064; HOLODEX-204) |
| [070](ADR-070-canary-release-candidate-and-promote-by-retag.md) | **Canary the release candidate, promote by retag** — `:edge` already *is* the open release PR's content (release-please stacks version bumps, not code), so no RC image is built; an **advisory** comment on the release PR carries the core + sidecar digests, their **per-image** freshness (commits touching *that image's own* `paths:` filter since it was built — not distance from `main` HEAD, which flags a correct rarely-rebuilt image as stale), and a pinned pull command; the canary compose pins by **digest** (rollback = previous digest); `release.yml` stops rebuilding at the tag and instead `buildx imagetools create`-retags the canaried digest to semver + `latest`, guarded by a load-bearing **ancestry assertion** (the digest's revision must be an ancestor of the tagged commit) that replaces ADR-035's tag-time rebuild gate | Proposed (extends ADR-023/024/034; amends ADR-035 release test-gate + ADR-044 promotion; HOLODEX-206) |
| [071](ADR-071-job-run-attribution-and-paginated-history.md) | **Job runs attribute to an entity; history reads through a keyset cursor** — `job_runs` gains a polymorphic `(entity_type, entity_id)` pair with **no foreign key** (an audit row must survive deletion of what it describes) plus a `batch_id` column, so "what touched video #412" is one indexed query and the Revert control reads a field instead of regexing `detail` — which fixes merge-propagation batches, whose `merge-person-N-M` id `/· batch (\d+)/` never matched. Reads split into a bounded per-kind digest and a **keyset**-paginated log over `(started_at, id)`, tuple-compared so a burst sharing one timestamp can't drop rows at a page boundary; a malformed cursor yields the first page, never a 400 | Proposed (extends ADR-028/067; relates ADR-033/047/048/061; spec job-history-digest-and-search; HOLODEX-207) |
| [072](ADR-072-person-link-resolved-derivation.md) | **Person link resolved-derivation** — `video_people` migrates from scan-time raw-extraction to **resolved-value derivation** (`RelinkVideoEntity`, sole writer; `RelinkVideoStudios` generalized in) with a **`role` column derived from the source person-typed field** (PK → `(video_id, person_id, role)`); **`entity: person` field marker** drives derivation/`peopleKeys`/link-picker; **lossless cutover** (derivation sources ⊇ extraction `peopleKeys`, backfill loss-guard); **30-day orphan grace + authored-identity prune guard** (vs. studio's immediate prune); `role` unset-capable (empty sentinel); writeback reuses `actors→Artist` (canonical name, no new perimeter); **studio parity (P0, ships with people)** adds owner-view studio link + `studio→Publisher` writeback | Proposed (applies+generalizes ADR-053, **amends its no-studio-writeback non-goal**; extends ADR-052/051/013; relates ADR-036/041/030/055; spec F40; foundation for F32) |
| [073](ADR-073-post-write-baseline-resync.md) | **Post-write read-back is unconditional; queued writes expose a job status** — `in_sync` compares a decided value against the DB's *stored copy* of the file's tags (ADR-052), and nothing refreshed that copy after a write, so the media page kept asserting "N out of sync" for fields already on disk until an unrelated rescan; the post-write `refresh.ReExtract` hook therefore fires after **every** write (gate + `MayIntroduceEntity` deleted, `PostWriteFunc` drops `fields`), including the merge/revert writes ADR-068 D1 excluded — the case where a stale baseline is worst, not least. Re-reading beats patching stored tags from our own inputs (tool normalization would make `in_sync` flap). Separately, ADR-048's queue answers **202 at enqueue**, so a refetch on that answer always reads pre-write state: adds owner-gated `GET /writeback/jobs/{id}` (absent row = `done`, since success deletes it) and a backing-off SPA poll that gates "applied" on the write actually landing | Proposed (supersedes **ADR-068 D1** only; extends ADR-048/041/047; relates ADR-051/052; HOLODEX-214) |
| [074](ADR-074-claimed-provider-keys.md) | **Claimed provider keys — a field's own source list is the sole suppression authority** — ADR-056 suppresses an auto-registered key by comparing the **canonical name** against the **raw provider key**, two different namespaces, so `provA:synopsis → overview` renders as a duplicate row and only a spelling coincidence (`tmdb:overview`) ever suppressed correctly. A canonical field may now **claim** a differently-named key: the key contributes as a candidate and stops auto-registering. Claims are authored two ways — operator `sources:` (video-only YAML) and an owner-gated `field_claims` store (migration 0029, PK carries **`provider`** so `provA:synopsis` claimed leaves `provB:synopsis` alone) — that materialize into the same `mapping.Source` before anything reads them. The load-bearing choice: suppression derives from the **merged `[]mapping.Field`**, not from the claims table, so one path covers YAML + synthesized + promoted fields *and* a dangling claim structurally cannot hide a value with nowhere to go. Claims carry **no precedence** (append last, lexicographic) — identity is not precedence; ADR-051 decisions stay the winner-picking instrument. Not a presentation tier; grants no new curation. **Not purely additive** — a key already listed under `sources:` stops auto-registering | Proposed (amends ADR-056 §D4; extends ADR-062/013/052; passes ADR-039/030/046; relates ADR-051/055; spec F49; HOLODEX-218 / GH #178) |
| [075](ADR-075-tag-governance-and-video-enrichment.md) | **Tag governance & video enrichment — hierarchy column, deny-list table, provenance-aware rescan, write-on-resolve materialization** — tags gain a strict one-parent-per-tag hierarchy (`tags.parent_tag_id`, application-layer cycle guard, query-time `WITH RECURSIVE` expansion, no closure table) and a global exact-match deny-list (`denied_tags`, a third and genuinely different-arity durable-negative-assertion table vs. ADR-061's `entity_keep_separate`/ADR-066's `enrichment_dismissals`), enforced inside `resolveOrCreateByName` so scan/manual/enrichment all route through one choke point. **Highest-risk piece:** `video_tags` gains a `source` column (reusing the existing `fieldsource` grammar) because `replaceAssociations()` today unconditionally deletes and rebuilds `video_tags` from the file on every rescan — harmless only because tags have never had a second source; fixed to a partial `source='file'`-scoped replace before that stops being true. Video-genre materialization hooks into the existing `afterEnrichApply` dispatcher (covers manual apply + Refresh + Refresh-all for free) and is named explicitly as **write-on-resolve**, distinct from ADR-063's pure compute-on-read `Derive` pass it otherwise resembles | Proposed (extends ADR-061/066; relates ADR-041/013/063; spec F50) |
| [076](ADR-076-commit-type-scope-advisory-check.md) | **Advisory CI check — flag `docs`/`chore`-typed PRs whose diff touches non-doc code** — PR #187 was titled `docs(tags): ...` but shipped the full F50 implementation (3 migrations, ~15 Go files, 5 Svelte files), hiding it from the `git-cliff`/`release-please` changelog (by design for `docs`/`chore`) and stalling HOLODEX-224's children at `In Progress`. A new workflow reads the **PR title** (what the squash-merge commit actually carries) against the PR's changed-file globs (`internal/**/*.go`, `cmd/**/*.go`, `providers/**/*.go`, `web/src/**/*.ts,*.svelte`, `internal/db/migrations/**`), flagging via a sticky PR comment — never a blocking check — when the type is `docs`/`chore` but more than one non-doc file changed, or any migration file did | Proposed (relates ADR-058/069/034; HOLODEX-238) |
| [077](ADR-077-tag-writeback-exclusion.md) | **Tag writeback exclusion — per-tag Genre writeback flag + manual sync batch seam** — `tags.writeback_enabled` (default on) is filtered into `TagNamesForVideo`'s final projection, flat per-name rather than hierarchy-inherited (the recursive ancestor walk itself is unchanged); the manual sync trigger batch-enqueues via a `propagateMerge`-style shared-`batchID` `EnqueueMany`, but recomputes `GenreWritebackValues` per video instead of reusing a precomputed name list, since `genres` is a computed union whose inputs the flag itself changes; a new `GET /writeback/batches/{batchID}/status` aggregates the existing `writeback_queue`/`job_runs` `batch_id` columns so an N-video batch has one progress poll instead of N | Proposed (extends ADR-075/048/067/073; relates ADR-041/013; spec tag-writeback-exclusion; HOLODEX-239) |
| [078](ADR-078-tag-categories-entity.md) | **Tag Categories — a deliberately reduced entity, its junction shape, and cross-table name collision** — `categories(id, name)` mirrors `tags`' original pre-identity shape (no provenance/alias/merge), deliberately kept **outside** `resolveOrCreateByName`'s `identityQueryByType` spine since Category never hits the scanner-driven-duplicate problem that spine amortizes; `category_tags(category_id, tag_id)` mirrors `video_tags` exactly (composite PK, both sides `ON DELETE CASCADE`, no provenance column — every row has exactly one write path). The one genuinely new pattern: a category can't share a name with a tag or vice versa, enforced via **paired DB triggers** on both tables (not an app-layer-only check) — the same posture this codebase already takes for same-table `nameKey` uniqueness (real unique indexes, not conventions), extended to a two-table case SQLite can express, unlike ADR-075 D1's cycle guard. The browse-facet expansion (category → member tag ids) slots into `VideoFilter.build()`'s existing `TagIDs` `EXISTS(...)` clause shape with no new query primitive | Proposed (extends ADR-061; relates ADR-075; spec tag-categories; HOLODEX-240) |
| [079](ADR-079-studio-image-roles.md) | **Studio image roles — entity-generic asset orchestration, retiring the `logo` field** — `studio_images(studio_id, role, source, ...)` (migration 0036, three core roles `icon`/`logo`/`poster`, no gallery) replaces `studio_logos`; `enrich.ImageSink`/`downloadAssets`/`assetRoleFor` widen from person-only to entity-generic (`entityType` parameter, Person behavior unchanged) at their second real use; TMDB company enrichment switches its logo from a resolved `image_url` **field** to an **asset**, so the studio `logo` field (and its ADR-051 decision) is retired in favor of owner upload + ADR-049-style provenance lock | Proposed (supersedes ADR-057, realizes its deferred Option D; extends ADR-038/039/049; relates ADR-051/052/061; spec F51; HOLODEX-247) |
| [080](ADR-080-configurable-provider-search-patterns.md) | **Configurable per-provider search query patterns** — core renders a `{studio?} {title?} {performers?} {year?}` template into the existing single-string `hint.query` before the unchanged `/resolve` call (no protocol bump); three-tier precedence (operator `search_pattern` override > provider-advertised `preferred_search_pattern` via additive `/describe` key > operator `default_search_pattern`), video-only (Person/Studio lack the structured fields to fill a template); the raw-title floor and the `{title}` token are additionally run through an **unconditional** sanitizer stripping bracket/comma punctuation and resolution tokens (`720p`, `4k`, …) — the one behavior change with no config gate, since it targets the freshly-scanned, un-enriched case with the least structured data and the most to gain | Proposed (extends ADR-033/013; precedent ADR-056; relates ADR-051/060/067; spec F53 pending; HOLODEX-254) |
| [081](ADR-081-entity-completeness-score.md) | **Entity completeness score — facet criticality, not-applicable status, and score computation** — `registry.FieldDef` gains a static `Criticality` (critical/nice-to-have, reusing the existing `Computed` exclusion); the tri-state not-applicable status persists in a new dedicated `facet_not_applicable(entity_type, entity_id, canonical_field)` table (mirrors `person_image_suppressions`, ADR-043) rather than a 4th `field_source_decisions` source — not-applicable answers relevance, not which source wins; score/actionability are a pure compute-on-read post-pass keyed off `ResolvedField.WinningSource` (twin of ADR-063's `Derive`), and browse/queue list views resolve+score the full per-type entity set in Go rather than pushing sort/filter into SQL, bounded by this app's personal-library scale; `imdb_id` is renamed to `external_provider_id` via a straight data migration across every `field_key`-keyed table, since production never populated the old name | Proposed (extends ADR-051/052/063; relates ADR-013/033; spec F55; HOLODEX-260) |

> **Reserved:** ADR-029 — live activity transport (Server-Sent Events) for F21.8 (P1), to be drafted when SSE is scheduled.

## Phase specs
- [Phase 1 — MVP](../specs/phase-1-mvp.md)
- [Phase 2 — MCP + Polish](../specs/phase-2-mcp-polish.md)
- [Phase 3 — Enrichment](../specs/phase-3-enrichment.md)
- [System Activity — "Under the Hood" (F21)](../specs/system-activity.md)
- [Quick Wins — Search history & "More with …" shelves](../specs/quick-wins.md)
- [Metadata Source Plugins (F22)](../specs/metadata-plugins.md) — keystone of Phase 3; detailed F16
- [Person Aliases (F23)](../specs/person-aliases.md) — first Phase-3 People slice; detailed F14.1
- [Sticky sort + Random sort](../specs/sort-persistence.md) — per-page localStorage sort + seeded Random (ADR-045)
- [Refresh Metadata (F31)](../specs/metadata-refresh.md) — per-item forced re-extract + re-enrich (ADR-047)
- [Per-field source-of-truth (F36)](../specs/field-source-of-truth.md) — standing per-item, per-field decision over precedence; file-first default (ADR-051)
- [Provider render hints (F39)](../specs/provider-render-hints.md) — `/describe.field_hints` + presence-driven, display-only auto-registration of non-canonical fields (ADR-056)
- [Runtime owner settings (F41)](../specs/runtime-settings.md) — owner-editable settings at runtime; DB override over startup config; debuts `person_gallery_max` (ADR-060)
- [WebP provider images (F42)](../specs/gallery-webp.md) — accept provider WebP through the existing person-image re-encode gauntlet (no ADR; no contract change)
- [Entity name-identity (F43)](../specs/entity-identity.md) — unified normalized `nameKey` + alias/merge across Person/Studio/Tag; case-collision fix + near-miss review queue (ADR-061)
- [In-app field promotion (F44)](../specs/promote-override-fields.md) — owner-gated in-app promote/override of an F39 auto-registered field into a first-class curatable field; DB `field_promotions` tier-0 over operator YAML (ADR-062)
- [Queryable fields substrate — Phase 1 (F46 Phase 1)](../specs/queryable-fields-substrate.md) — typed field registry (text/categorical/numeric/date) + equals/contains/range operator model, piloted via Age in Media (person × video, first relationship-scoped computed field, extends F45/ADR-063); compute-on-read, no persisted index (HOLODEX-176; ADR TBD)
- [Enrichment review workflow (F47)](../specs/enrichment-review-workflow.md) — entity-generic review queue, confidence-gated auto-apply, durable "not matched" dismissal, refresh bypass (ADR-066)
- [Metadata extraction from filenames (F48)](../specs/metadata-extraction.md) — on-demand/batch/import-time extraction with confidence-gated auto-apply + owner review queue; merge → writeback propagation; rollback backup/revert (ADR-067)
- [Claimed provider keys (F49)](../specs/claimed-provider-keys.md) — a canonical field claims a differently-named provider key so it contributes as a candidate instead of auto-registering as a duplicate row; YAML `sources:` + in-app claims for the entity types with no YAML (HOLODEX-218 / GH #178; ADR-074)
- [Tag governance & video enrichment (F50)](../specs/tag-governance-and-video-enrichment.md) — tag hierarchy + global deny-list + video-enrichment tag materialization + genre writeback, built on F43's identity spine; fixes a latent rescan data-loss gap (`video_tags` gains provenance) as its P0 (ADR-075)
- [Tag writeback exclusion](../specs/tag-writeback-exclusion.md) — per-tag boolean flag excluding a tag from file `Genre` writeback while it stays a normal searchable Holodex tag; manual (not automatic) per-tag/bulk sync to already-written files, reusing the writeback-modal pattern (ADR-077; HOLODEX-239)
- [Studio image roles (F51)](../specs/studio-images.md) — icon/logo/poster as independently owner-editable image slots (upload + enrichment, ADR-049-style provenance lock), replacing the single enrichment-only logo cache; realizes ADR-057's deferred Option D (ADR-079; HOLODEX-247)
- [Owner person & studio media linking (F40)](../specs/person-media-linking.md) — owner-authored person/studio ↔ video links via curation, `video_people` migrated to resolved-value derivation with a role column and 30-day orphan grace, writeback round-trip (ADR-072; HOLODEX-114)
- [Owner-mode video editing (F52)](../specs/video-owner-mode-editing.md) — Commentary field (zero-source manual-first replace field), poster upload as a new thumbnail-pipeline tier, studio moved next to the title, file metadata gated to owner-only; F40 implements the People/Studio-linking half of this same change (HOLODEX-251/HOLODEX-252)
- [Configurable provider search patterns (F54)](../specs/configurable-provider-search-patterns.md) — three-tier operator/provider/default query-pattern precedence over the existing single-string `hint.query`, plus an unconditional bracket/resolution title sanitizer for the raw-title floor (ADR-080; HOLODEX-254)
- [Entity completeness score (F55)](../specs/entity-completeness-score.md) — per-entity completeness score (weighted, tiered by source trust) + a separate actionability signal, owner-mode browse sort/filter, a facet-first remediation queue, a per-entity breakdown panel, and a tri-state not-applicable affordance scoped to video `external_provider_id` ([ADR-081](ADR-081-entity-completeness-score.md); HOLODEX-260)

## Cross-cutting
- [Testing Strategy](../testing-strategy.md) — pyramid, fixture corpus, per-component plan, CI, phasing
